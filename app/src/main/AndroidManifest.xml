<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CycleSync">

        <!-- Splash -->
        <activity
            android:name=".ui.splash.SplashActivity"
            android:theme="@style/Theme.CycleSync.Splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Auth -->
        <activity android:name=".ui.auth.SignUpActivity"/>
        <activity android:name=".ui.auth.LoginActivity"/>

        <!-- Onboarding (urutan flow) -->
        <activity android:name=".ui.onboarding.PersonalDataActivity"/>
        <activity android:name=".ui.onboarding.GoalSelectionActivity"/>
        <activity android:name=".ui.onboarding.LmpInputActivity"/>
        <activity android:name=".ui.onboarding.CalculatingActivity"/>

        <!-- Main App -->
        <activity android:name=".ui.dashboard.DashboardActivity"/>
        <activity android:name=".ui.dashboard.DailyLogActivity"/>
        <activity android:name=".ui.calendar.CalendarActivity"/>

        <activity android:name=".ui.activity.MainActivity" />
    </application>

</manifest>